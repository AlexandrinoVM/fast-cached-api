datasource db {
  provider = "postgresql"
}


generator cliente {
    provider = "prisma-client-js"
} 

model Itens {
    id Int @id @default(autoincrement())
    id_item Int @unique 
    name String @unique 
}

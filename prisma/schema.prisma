datasource db {
  provider = "postgresql"
}


generator cliente {
    provider = "prisma-client-js"
} 

model itens {
    id Int @id @default(autoincrement())
    id_item Int @unique 
    name String @unique 
    price Float
    description String
}
